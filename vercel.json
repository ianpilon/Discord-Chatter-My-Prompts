{
  "version": 2,
  "builds": [
    { "src": "client/**", "use": "@vercel/static" },
    { "src": "server/api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/api/$1.ts" },
    { "src": "/(.*)", "dest": "/client/$1" }
  ],
  "crons": [
    {
      "path": "/api/cron/auto-analysis",
      "schedule": "0 12 * * *"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
